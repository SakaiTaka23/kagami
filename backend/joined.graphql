type Post {
  id: String!
  content: String!
  user: User!
  userId: String!
}

type Query {
  timeline(take: Int!, cursor: String): [Post!]!
  postDetail(id: String!): Post
  userFromToken: User!
  user(id: String!): User
}

type User {
  id: String!
  accountName: String!
  userName: String!
}

input UserName {
  accountName: String!
  userName: String!
}

type Mutation {
  createUser(username: UserName!): User!
}

schema {
  query: Query
  mutation: Mutation
}