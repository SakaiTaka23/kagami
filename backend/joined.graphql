type User {
  id: String!
  name: String!
}

input UserName {
  name: String!
}

type Query {
  userFromToken: User!
  user(id: String!): User
}

type Mutation {
  createUser(username: UserName!): User!
}

schema {
  query: Query
  mutation: Mutation
}