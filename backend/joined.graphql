type Post {
  id: String!
  content: String!
  user: User!
  userId: String!
}

type Query {
  timeline(take: Int!, cursor: String): [Post!]!
  userFromToken: User!
  user(id: String!): User
}

type User {
  id: String!
  account_name: String!
  user_name: String!
}

input UserName {
  account_name: String!
  user_name: String!
}

type Mutation {
  createUser(username: UserName!): User!
}

schema {
  query: Query
  mutation: Mutation
}