type Post {
  id: String!
  content: String!
  user: User!
  userId: String!
}

type Query {
  timeline(take: Int!, cursor: String): [Post!]!
  postDetail(id: String!, userName: String!): Post
  userFromToken: User!
  userFromUserName(userName: String!): User
  user(id: String!): User
}

type User {
  id: String!
  accountName: String!
  userName: String!
}

type Follow {
  followerId: String!
  followingId: String!
}

input UserName {
  accountName: String!
  userName: String!
}

type Mutation {
  createUser(username: UserName!): User!
  followToggle(followingId: String!): Follow!
}

schema {
  query: Query
  mutation: Mutation
}