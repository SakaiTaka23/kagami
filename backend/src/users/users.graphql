type User {
  id: String!
  accountName: String!
  userName: String!
}

input UserName {
  accountName: String!
  userName: String!
}

type Query {
  userFromToken: User!
  user(id: String!): User
}

type Mutation {
  createUser(username: UserName!): User!
}
