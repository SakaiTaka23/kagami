type User {
  id: String!
  account_name: String!
  user_name: String!
}

input UserName {
  account_name: String!
  user_name: String!
}

type Query {
  userFromToken: User!
  user(id: String!): User
}

type Mutation {
  createUser(username: UserName!): User!
}
