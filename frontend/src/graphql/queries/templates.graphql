query TemplateDetail($templateDetailId: String!) {
  templateDetail(id: $templateDetailId) {
    content
    detail
    createdAt
    updatedAt
    user {
      accountName
      userName
    }
  }
}
